/**
 * @file 地下模式配置, 无效, 没有使用
 */
define( [

], function( ) {
    'use strict';

    return {
        globe: {
            globeAlpha: 0.01
        },
        undergroundMode: true,
        screenSpaceCameraController: {
            minimumZoomDistance: -1000
        }
    };
} );
