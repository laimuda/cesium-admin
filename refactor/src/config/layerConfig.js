/**
 * @file 图层的配置信息
 */
define( [

], function( ) {
    'use strict';

    return {
        build: {
            brightness: 1.8,
            contrast: 1.1,
            saturation: 1.2,
            gamma: 1.3
        }
    };
} );
